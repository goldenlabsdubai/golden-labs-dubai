/**
 * @deprecated Firestore was removed. Export data from Firebase Console if needed, then import manually
 * or use SQL COPY. This script no longer runs.
 */
console.error(
  "migrate-firestore-to-postgres.js is deprecated (Firestore removed). Use PostgreSQL only; see scripts/postgres-schema.sql."
);
process.exit(1);
